<div class="container">
  <div class="row">
    <div class="col-8 offset-2 mt-3">
      <div class="index-wrapped">
        <% if @range == "User" %>
          <h4 style="background-color: #99ff99;", class="text-white pl-5">ユーザー検索結果 "<%= @word %>"</h4>
          <table class="table table-borderless">
            <tbody>
              <% @users.each do |user| %>
                <% if user.is_deleted == false %>
                  <tr>
                    <td>
                      <%= link_to public_user_path(user) do %>
                        <%= image_tag user.get_profile_image(50, 50) %>
                      <% end %>
                    </td>
                    <td>
                      <%= link_to public_user_path(user) do %>
                        <%= user.name %>
                      <% end %>
                    </td>
                  </tr>
                <% end %>
              <% end %>
            </tbody>
          </table>

        <% else %>
          <h4 style="background-color: #99ff99;", class="text-white pl-5">ゴフノベル検索結果 "<%= @word %>"</h4>
          <table class="table table-borderless">
            <tbody>
              <% @novels.each do |novel| %>
              <% if novel.user.is_deleted == false %>
                <tr>
                  <td>
                    <%= link_to public_novel_path(novel) do %>
                      <%= novel.title %>
                    <% end %>
                  </td>
                  <td>
                    <%= link_to public_user_path(novel.user) do %>
                      <%= novel.user.name %>
                    <% end %>
                  </td>
                </tr>
              <% end %>
              <% end %>
            </tbody>
          </table>
        <% end %>
      </div>

    </div>
  </div>
</div>
